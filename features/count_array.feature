Feature:Identifying  the Array Length

    Scenario: Count Array elements
        Given :array is empty
        Then :length of array should be zero
        When :the array is loaded with elements 2,3,4
        Then :the length of array should be 3
